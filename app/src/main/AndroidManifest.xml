<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.haochat">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ApplicationTrans"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DengluActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".tongxinActivity" />
        <activity android:name=".FreindActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".chat" />
        <activity android:name=".gerenziliao" />
        <activity android:name=".friendquan" />
        <activity android:name=".sendfriendquan"></activity>
    </application>

</manifest>